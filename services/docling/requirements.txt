# Docling Service Dependencies
#
# Versions are pinned to a known-good, performance-verified combination.
# Fresh installs must be deterministic — unpinned upgrades have caused
# severe regressions (sentence-transformers 6.0: embedding ~20x slower).

# Document parsing
docling==2.117.0

# Embeddings (6.0 measured ~20x slower than 5.6.1 — do not upgrade blindly)
sentence-transformers==5.6.1

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Image processing (for docling image extraction)
Pillow>=10.0.0
